spring:
  jpa:
    show-sql: false

logging:
  level:
    br.com.useautentica.backend: INFO

# Configurações adicionais de produção (logs estruturados, monitoramento,
# secrets via variáveis de ambiente do provedor de cloud) serão detalhadas
# na V3 do roadmap (Infraestrutura e produção).
